mkdir /opt/app-root/moodledata

#chown -R apache. /opt/app-root/moodledata
chmod -R 777 /opt/app-root/moodledata

#!/bin/bash
mv .  mv /opt/app-root/src/


   # mv . * $HOME/.
    
    # mv  . $HOME/.


#mover el moodle
#mv /tmp/s2i/src $HOME/src

# build application artifacts
pushd ${HOME}
make all

# install the artifacts
make install
popd
